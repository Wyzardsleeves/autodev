{
  "id": "T-003",
  "type": "feature",
  "title": "Add a count endpoint for the todo list",
  "description": "Add GET /todos/count returning {\"total\": n, \"completed\": n, \"pending\": n} so the UI can show progress without fetching every todo."
}
