{
  "id": "T-004",
  "type": "feature",
  "title": "Search todos by title",
  "description": "Add an optional q query parameter to GET /todos that narrows results to todos whose title contains the given text, case-insensitively. It should combine with the existing completed filter."
}
