{
  // The root is now the autodev_agent project (pyproject.toml + .venv live here).
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "python.testing.pytestEnabled": false
}
